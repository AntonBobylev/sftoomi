framework:
    secret: '%env(APP_SECRET)%'

    session:
        handler_id: null
        storage_factory_id: session.storage.factory.native
        cookie_secure: auto
        cookie_samesite: lax
        cookie_lifetime: 3600
        gc_maxlifetime: 3600
